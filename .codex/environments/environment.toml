# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "main-base"

[setup]
script = ""

[setup.darwin]
script = '''
#!/usr/bin/env bash
set -euo pipefail

BRANCH="main"

SOURCE_TREE="$CODEX_SOURCE_TREE_PATH"
WORKTREE="$CODEX_WORKTREE_PATH"

git -C "$SOURCE_TREE" reset --hard HEAD
git -C "$SOURCE_TREE" clean -fd
git -C "$SOURCE_TREE" fetch origin "$BRANCH"
git -C "$SOURCE_TREE" checkout "$BRANCH"
git -C "$SOURCE_TREE" reset --hard "origin/$BRANCH"

git -C "$WORKTREE" fetch origin "$BRANCH"
git -C "$WORKTREE" reset --hard "origin/$BRANCH"'''
